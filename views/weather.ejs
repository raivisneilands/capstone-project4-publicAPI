<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Check The Weather</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/styles.css">
</head>
<body class="weather-body">
    <form action="/switch-to-jokes" method="post" class="text-center my-3 ">
        <input type="submit" value="Jokes" name="jokes" class="btn btn-primary">
    </form>
    <form action="/check-weather" method="post" class="weather-form">
        <label for="city">Enter the city: </label>
        <input type="text" name="city">
        <input type="submit" value="Check The Weather" class="btn btn-primary">
    </form>
    <h2>
        <div class="container">
            <div class="row">
                    <% if (locals.temperature && locals.description && locals.icon) { %>
                        <div class="current p-3 m-3 text-center">
                            <div class="city">
                                <h5 class="display-5">
                                    <%= location %>
                                </h5>
                            </div>
                            <div class="temperature my-3">
                                <%= temperature %>°C
                            </div>
                            <div class="description my-3">
                                <%= description %>
                            </div>
                            <div class="icon my-3">
                                <img src="<%= icon %>" alt="weather icon">
                            </div>
                        </div>
                    <% } else { %>
                        <h3 class="display-4 text-center m-5">
                            Enter the and click "Check The Weather" button
                        </h3>
                    <% } %>
            </div>
        </div>
    </h2>
</body>
</html>